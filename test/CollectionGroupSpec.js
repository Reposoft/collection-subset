'use strict';

var expect = require('chai').expect;

module.exports = function interfaceSpec(required) {

  describe("#group", function() {

    it("Returns a model like object to observe if there are matches to a filter", function() {

    });

    it("Provides no access to these matching models, just hints of existence", function() {

    });

    it("Has an attribute empty =true/false", function() {

    });

    it("Emits change:empty event as a first model enters or exits the group", function() {

    });

    xit("Has an attribute size, value 0 for empty", function() {

    });

  });

}
