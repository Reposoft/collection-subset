'use strict';

var expect = require('chai').expect;

module.exports = function interfaceSpec(required) {

  describe("filter:enum", function() {

    xit("Is modelled on PourOver's makeExactFilter");
    xit("Can be defined with two arguments, a string key name and an array of allowed values");
    xit("Is query string compatible with an implicit OR between parameters of the same name");

  });

};
