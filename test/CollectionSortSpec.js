
module.exports = function interfaceTest(required) {

  describe("Sorting and guards against sort", function() {

    xit("Forbids the classic Backbone .sort because persistence should not be affected by presentation sorting", function() {

    });

    xit("A way to disconnect from the underlying order so a regular backbone .sort() can run safely", function() {

    });

    xit("How this makes ordered insertion throw unsupported messages", function() {

    });

  });

};
